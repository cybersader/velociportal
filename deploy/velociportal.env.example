# Copy to velociportal.env and restrict it to TrueNAS administrators plus the
# Apps service. Values use Velociportal's go-quoted-v1 encoding.

# These names must be private aliases on the internal velociportal-upstreams
# Docker network. Do not expose either credentialed API through public DNS.
HEADSCALE_URL="http://headscale.velociportal.internal:8080"
HEADSCALE_API_KEY="replace-with-the-dedicated-velociportal-api-key"
NPM_URL="http://npm.velociportal.internal:81"
NPM_EMAIL="velociportal@example.com"
NPM_PASSWORD="replace-with-the-dedicated-npm-password"

POLL_INTERVAL="30s"
